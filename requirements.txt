# Core Dependencies - CRÍTICAS PARA EJECUCIÓN
numpy>=2.1.0
scikit-learn>=1.5.0
python-dotenv>=1.0.0

# Embeddings - CRÍTICO (sentence-transformers para embeddings de calidad local)
sentence-transformers>=3.0.0

# Vector Search - RECOMENDADO (FAISS para búsqueda vectorial eficiente)
faiss-cpu>=1.8.0

# OpenAI Integration - OPCIONAL (solo si deseas usar API de OpenAI)
openai>=1.54.0
